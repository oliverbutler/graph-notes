// Types of property fields
export enum PropertyType {
  Text = 'text',
  Number = 'number',
  Phone = 'phone',
}

// Property Enums
export enum PropertyUnits {
  GBP = 'pound_sterling',
  USD = 'united_states_dollar',
  Euro = 'euro',
}
